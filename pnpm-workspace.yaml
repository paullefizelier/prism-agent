# Build scripts are ignored by default in pnpm 10+. Only the packages listed here
# may run their install/build scripts. `sharp` (used by @nuxt/image) needs it to
# fetch its native libvips binary; the others ship prebuilt platform binaries and
# don't need a build step.
onlyBuiltDependencies:
  - sharp
