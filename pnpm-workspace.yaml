# pnpm ignores dependency build scripts by default and, in CI, errors unless each
# one is explicitly allowed (true) or denied (false). These all ship native
# binaries / build steps and are safe to build.
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  sharp: true
  unrs-resolver: true
  vue-demi: true
