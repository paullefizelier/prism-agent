# Build scripts are ignored by default in pnpm 10+, and in CI pnpm errors unless
# every dependency with a build script is explicitly listed here. These all ship
# native binaries / build steps and are safe to build.
onlyBuiltDependencies:
  - '@parcel/watcher'
  - esbuild
  - sharp
  - unrs-resolver
  - vue-demi
